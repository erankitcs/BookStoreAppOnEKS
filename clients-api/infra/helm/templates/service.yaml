apiVersion: v1
kind: Service
metadata:
  name: {{ include "booksotre-clients-api.fullname" . }}
  labels:
    {{- include "booksotre-clients-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "booksotre-clients-api.selectorLabels" . | nindent 4 }}
